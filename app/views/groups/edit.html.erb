<%= form_for @group do |f| %>
  <%= f.text_field :name %>
  <%= f.collection_check_boxes :user_ids, User.all,
    :id, :name, {}, {disabled: false} do |ck| %>
      <%= ck.label(class: "check_box"){ck.check_box(class: "ck") + ck.text} %>
  <% end %>
  <%= f.submit "Save" %>
<% end %>
